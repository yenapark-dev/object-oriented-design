using System;

namespace FileComparison
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}

//Create a program named FileComparison that compares two files.
//First, use a text editor such as Notepad to save your favourite
//movie quote. Next, copy the file contents, and paste them into a
//word-processing program such as Word. Then, write the
//file-comparison application that displays the sizes of the
//two files as well as the ratio of their sizes to each other.
//To discover a file’s size, you can create a System.IO.FileInfo
//object using statements such as the following, where FILE_NAME
//is a string that contains the name of the file, and size has been
//declared as an integer: